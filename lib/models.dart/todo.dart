/// The Todo class represents a task with an id, title, and description.
class Todo {
  String id;
  String title;
  String description;

  Todo({
    required this.id,
    required this.title,
    required this.description,
  });
}


